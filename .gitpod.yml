tasks:
  - init: |
      sudo apt install cmake --assume-yes
      chmod +x scripts/build.sh scripts/clean-build.sh

github:
  prebuilds:
    master: true
    branches: false
    addComments: true
    addCheck: true

vscode:
  extensions:
    - notskm.clang-tidy
    - twxs.cmake
